#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# --ensure-latest queries RubyGems and fails as soon as a newer Brakeman is
# published, so it only runs outside CI to keep builds reproducible.
ARGV.unshift("--ensure-latest") unless ENV["CI"]

load Gem.bin_path("brakeman", "brakeman")
