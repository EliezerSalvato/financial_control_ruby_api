---
en:
  activemodel:
    errors:
      models:
        core/institution/creation/input:
          attributes:
            base:
              institution_creation_failed: Institution creation failed
        core/institution/deletion/input:
          attributes:
            base:
              institution_destruction_failed: Institution deletion failed
              institution_not_found: Institution not found
              used_by_transactions: This institution cannot be deleted because it has accounts or credit cards already used by transactions. To stop using
                it, inactivate the institution.
        core/institution/update/input:
          attributes:
            base:
              institution_not_found: Institution not found
              institution_update_failed: Institution update failed
  institution:
    creation:
      failure: Institution creation failed
      success: Institution created successfully
    deletion:
      failure: Institution deletion failed
      success: Institution deleted successfully
    errors:
      invalid_filters: Invalid filter
      not_found: Institution not found
    update:
      failure: Institution update failed
      success: Institution updated successfully
