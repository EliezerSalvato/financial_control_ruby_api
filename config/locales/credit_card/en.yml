---
en:
  activemodel:
    errors:
      models:
        core/credit_card/creation/input:
          attributes:
            base:
              credit_card_creation_failed: Credit card creation failed
        core/credit_card/deletion/input:
          attributes:
            base:
              credit_card_destruction_failed: Credit card deletion failed
              credit_card_not_found: Credit card not found
              used_by_transactions: This credit card cannot be deleted because it is already used by transactions. To stop using it, inactivate the credit
                card.
        core/credit_card/invoice_settlement/creation/input:
          attributes:
            base:
              available_limit_exceeds_total_limit: Available limit would exceed total limit
              insufficient_account_balance: Insufficient account balance
              invoice_settlement_creation_failed: Invoice settlement failed
        core/credit_card/invoice_settlement/ensure_unpaid/input:
          attributes:
            base:
              invoice_already_paid: Expenses cannot be posted to this card because the invoice for this period has already been paid
            credit_card_id:
              not_found: was not found
        core/credit_card/update/input:
          attributes:
            base:
              credit_card_not_found: Credit card not found
              credit_card_update_failed: Credit card update failed
            total_limit:
              insufficient_available_limit: would result in a negative available limit
  credit_card:
    creation:
      failure: Credit card creation failed
      success: Credit card created successfully
    deletion:
      failure: Credit card deletion failed
      success: Credit card deleted successfully
    errors:
      available_limit_exceeds_total_limit: Available limit would exceed total limit
      insufficient_available_limit: Insufficient available limit
      invalid_filters: Invalid filter
      invalid_period: Invalid period
      not_found: Credit card not found
    update:
      failure: Credit card update failed
      success: Credit card updated successfully
