---
en:
  activemodel:
    errors:
      models:
        core/user/account/deletion/input:
          attributes:
            base:
              account_deletion_failed: Account deletion failed
        core/user/authentication/input:
          attributes:
            base:
              invalid_email_or_password: Email or password is invalid
              refresh_token_creation_failed: Refresh token creation failed
              session_creation_failed: Session creation failed
              sessions_revocation_failed: Previous sessions revocation failed
              signed_token_creation_failed: Signed token creation failed
              user_email_is_not_verified: User email is not verified
              user_is_not_active: User is not active
        core/user/email/change/input:
          attributes:
            base:
              change_email_and_mark_as_not_verified_failed: Change email and mark as not verified failed
              email_confirmations_invalidation_failed: Email confirmations invalidation failed
              new_email_is_the_same_as_the_current_email: New email is the same as the current email
              sessions_revocation_failed: Sessions revocation failed
        core/user/email/confirmation/input:
          attributes:
            base:
              invalid_token: Token is invalid
              mark_email_confirmation_as_confirmed_failed: Mark email confirmation as confirmed failed
              mark_user_as_verified_failed: Mark user as verified failed
        core/user/password/change/input:
          attributes:
            base:
              sessions_revocation_failed: Sessions revocation failed
            current_password:
              invalid: is invalid
            password:
              same_as_current_password: is the same as the current password
        core/user/password/reset/input:
          attributes:
            base:
              invalid_token: Token is invalid
              mark_password_reset_as_reset_failed: Mark password reset as reset failed
              sessions_revocation_failed: Sessions revocation failed
              user_email_is_not_verified: User email is not verified
              user_is_not_active: User is not active
            password:
              same_as_current_password: is the same as the current password
        core/user/password/reset/send_instructions/input:
          attributes:
            base:
              password_resets_invalidation_failed: Password resets invalidation failed
              user_email_is_not_verified: User email is not verified
              user_is_not_active: User is not active
            email:
              not_found: not found
        core/user/profile/update/input:
          attributes:
            base:
              at_least_one_attribute: Must include at least one of first_name, last_name, or configs
        core/user/session/refresh/input:
          attributes:
            base:
              invalid_refresh_token: Refresh token is invalid
              refresh_token_creation_failed: Refresh token creation failed
              refresh_token_update_failed: Refresh token update failed
              signed_token_creation_failed: Signed token creation failed
              user_is_not_active: User is not active
        core/user/session/revoke/input:
          attributes:
            base:
              sessions_revocation_failed: Sessions revocation failed
  user:
    account:
      deletion:
        failure: Account deletion failed
        success: Account deleted successfully
    authentication:
      failure: Authentication failed
    email:
      change:
        failure: Email change failed
        success: Email confirmation request sent
      confirmation:
        failure: Email confirmation failed
        success: Email confirmed successfully
    errors:
      internal_server_error: Internal Server Error
      unauthorized: Unauthorized
    password:
      change:
        failure: Password change failed
        success: Password changed successfully
      reset:
        failure: Password reset failed
        success: Password reset successfully
      reset_instructions:
        failure: Password reset instructions failed
        success: Password reset instructions sent successfully
    profile:
      update:
        failure: Profile update failed
        success: Profile updated successfully
    registration:
      failure: Registration failed
      success: Registration successful, please check your email to confirm your account
    session:
      refresh:
        failure: Session refresh failed
      revoke:
        failure: Session revocation failed
        success: Session revoked successfully
