patch:
  tags: [User]
  summary: Change password
  description: Changes the password of the authenticated user. Requires the current password.
  operationId: changePassword
  security:
    - bearerAuth: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas.yml#/PasswordChangeRequest"
        example:
          current_password: password123
          password: new-password123
          password_confirmation: new-password123
  responses:
    "200":
      description: Password changed
      content:
        application/json:
          schema:
            $ref: "../../components/schemas.yml#/MessageSuccessResponse"
    "422":
      $ref: "../../components/responses.yml#/UnprocessableContent"
    "500":
      $ref: "../../components/responses.yml#/Unauthorized"
